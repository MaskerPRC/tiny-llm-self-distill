{
  "name": "tinybert-pipeline",
  "version": "1.0.0",
  "description": "自迭代LLM驱动的Agent服务系统 - 自适应蒸馏、训练、工具生成与流程自修改",
  "main": "server/index.js",
  "scripts": {
    "dev:server": "node server/index.js",
    "dev:client": "cd client && npm run dev",
    "dev": "concurrently \"npm run dev:server\" \"npm run dev:client\"",
    "build": "cd client && npm run build",
    "setup": "npm install && cd client && npm install && pip install -r training/requirements.txt",
    "train:deps": "pip install -r training/requirements.txt"
  },
  "dependencies": {
    "express": "^4.21.0",
    "cors": "^2.8.5",
    "better-sqlite3": "^11.6.0",
    "multer": "^1.4.5-lts.1",
    "axios": "^1.7.7",
    "dotenv": "^16.4.5",
    "uuid": "^10.0.0",
    "ws": "^8.18.0",
    "onnxruntime-node": "^1.20.0",
    "concurrently": "^9.0.1"
  }
}
